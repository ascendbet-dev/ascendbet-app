DEVELOPMENT RULES

Code like a senior 10x mobile-first product engineer.

UI must be Apple-inspired minimalism.

No linear gradients allowed anywhere in the UI.

UI must strictly follow the tokens defined in DESIGN_SYSTEM.md.

No hardcoded color values allowed inside components.

Use calm purple glow shadows for emphasis.

Animations must be smooth and subtle.

Mobile-first layout only.

All business logic must run in Supabase Edge Functions.

Frontend must never enforce betting rules.

Do not implement features outside PRD scope.

Never refactor stake, odds, or DD logic.